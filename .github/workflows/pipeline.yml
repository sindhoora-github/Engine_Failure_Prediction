```
name: Predictive Maintenance pipeline

on:
  workflow_dispatch:

jobs:

  register-dataset:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Dependencies
        run: pip install -r pm_project/requirements.txt
      - name: Upload Dataset to Hugging Face Hub
        env:
          EFP_TOKEN: ${{ secrets.EFP_TOKEN }}
        run: python pm_project/model_building/data_register.py

  data-prep:
    needs: register-dataset
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Dependencies
        run: pip install -r pm_project/requirements.txt
      - name: Run Data Preparation
        env:
          EFP_TOKEN: ${{ secrets.EFP_TOKEN }}
        run: python pm_project/model_building/prep.py


  model-traning:
    needs: data-prep
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Dependencies
        run: pip install -r pm_project/requirements.txt
      - name: Model Building
        env:
          EFP_TOKEN: ${{ secrets.EFP_TOKEN }}
        run: python pm_project/model_building/train.py


  deploy-hosting:
    runs-on: ubuntu-latest
    needs: [model-traning,data-prep,register-dataset]
    steps:
      - uses: actions/checkout@v3
      - name: Install Dependencies
        run: pip install -r pm_project/requirements.txt
      - name: Push files to Frontend Hugging Face Space
        env:
          EFP_TOKEN: ${{ secrets.EFP_TOKEN }}
        run: python pm_project/hosting/hosting.py
